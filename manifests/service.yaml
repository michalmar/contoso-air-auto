apiVersion: v1
kind: Service
metadata:
  name: contoso-air-auto
  namespace: dev
  labels:
    app.kubernetes.io/name: contoso-air-auto
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: contoso-air-auto
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000